#include <iostream>
#include <cmath>

using namespace std;

const int MAXN = 10;

int mat[MAXN][MAXN] = {0};
int linesum[MAXN] = {0}, colSum[MAXN] = {0},

int main(){
	int n;
	cin >> n;
	int perUnit = (1 + n * n) * n / 2;

	return 0;
}
